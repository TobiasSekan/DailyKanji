<Window x:Class="DailyKanji.Mvvm.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DailyKanji"
        Width="1000"
        Height="450"
        x:ClassModifier="internal"
        DataContext="{Binding Path=ViewModel, RelativeSource={RelativeSource Mode=Self}}"
        WindowStartupLocation="CenterScreen">

    <Window.InputBindings>
        <KeyBinding Key="D1" Command="{Binding AnswerNumber}" CommandParameter="1" />
        <KeyBinding Key="D2" Command="{Binding AnswerNumber}" CommandParameter="2" />
        <KeyBinding Key="D3" Command="{Binding AnswerNumber}" CommandParameter="3" />
        <KeyBinding Key="D4" Command="{Binding AnswerNumber}" CommandParameter="4" />
        <KeyBinding Key="D5" Command="{Binding AnswerNumber}" CommandParameter="5" />
        <KeyBinding Key="D6" Command="{Binding AnswerNumber}" CommandParameter="6" />
        <KeyBinding Key="D7" Command="{Binding AnswerNumber}" CommandParameter="7" />
        <KeyBinding Key="D8" Command="{Binding AnswerNumber}" CommandParameter="8" />
        <KeyBinding Key="D9" Command="{Binding AnswerNumber}" CommandParameter="9" />
        <KeyBinding Key="D0" Command="{Binding AnswerNumber}" CommandParameter="0" />
    </Window.InputBindings>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="400" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="15" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Correct answers:" />

            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding Path=Model.RightAnswerCount}" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Wrong answers:" />
            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding Path=Model.WrongAnswers.Count}" />
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Rate:" />
            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding Path=Model.CurrentRateText}" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Possible answers:" />

            <ComboBox Grid.Row="4"
                      Grid.Column="2"
                      ItemsSource="{Binding Path=Model.ChoosableAnswerCountList}"
                      SelectedItem="{Binding Path=Model.MaximumAnswer}"
                      SelectionChanged="ComboBox_SelectionChanged" />

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Similar answers:" />

            <CheckBox Grid.Row="6"
                      Grid.Column="2"
                      Command="{Binding Path=ChangeAnswerMode}"
                      IsChecked="{Binding Path=Model.SimilarAnswers}" />

        </Grid>

        <TextBlock Grid.Row="0"
                   Grid.Column="1"
                   Width="200"
                   Height="200"
                   Padding="0,50,0,0"
                   HorizontalAlignment="Center"
                   Background="{Binding Path=Model.CurrentAskSignColor}"
                   FontSize="80"
                   Text="{Binding Path=Model.CurrentAskSign}"
                   TextAlignment="Center" />

        <StackPanel Grid.Row="0" Grid.Column="2">
            <TextBlock HorizontalAlignment="Center" Text="Error list" />
            <ScrollViewer>
                <DataGrid HeadersVisibility="Column" ItemsSource="{Binding Path=Model.WrongAnswers}" />
            </ScrollViewer>
        </StackPanel>

        <StackPanel x:Name="AnswerButtonArea"
                    Grid.Row="2"
                    Grid.ColumnSpan="3"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal" />
    </Grid>

</Window>
